/**
 *
 */
export default interface CompletedRequest {
  /**
   * RedirectURL is the URL which you should redirect the user to once the
   * authentication process is completed.
   */
  redirect_to?: string;
}
